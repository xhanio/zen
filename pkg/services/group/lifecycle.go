package group

// Intentionally empty: the Group service has no Init/Start/Stop work. The
// file exists to keep the framingo 5-file pattern intact and to give future
// lifecycle hooks an obvious home.
